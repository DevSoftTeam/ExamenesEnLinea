{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crear exámen</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear exámen" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('test_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
